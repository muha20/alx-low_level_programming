#include <stdio.h>
/**
 * main - print if the number is positive, zero, or nagative
 * Description: using the main function
 * this programe prints "Programming is positive, zero, or nagative
 * Return: 0
 */
int main(void)
{
	char c;

	for (c = 'z'; c >= 'a'; c--)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
