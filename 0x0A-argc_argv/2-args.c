#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
 * main - print name of the program
 * @argc: count arguments
 * @argv: Arguments
 *
 * Return: Alwas 0 (success)
 */

int main(int argc, char *argv[])
{
	int n;

	for (n = 0; n < argc; n++)
	{
		printf("%s\n", argv[count]);
	}
	return (0);
}
